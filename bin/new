#! /usr/bin/ruby

require 'fileutils'

now = Time.now
folder = now.strftime("entries/%Y/%m/%d/").downcase
filename = now.strftime("%H:%M:%S.md").downcase
path = folder + filename

FileUtils.mkdir_p folder
FileUtils.touch path

puts path
